import React, { Component } from 'react';
import PropTypes from 'prop-types';

class App extends Component{
    static propTypes = {}

    render(){
      return (
        <h1>React Hooks</h1>
      );
    }
}

export default App;
